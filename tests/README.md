# Test Layout

Frontend unit tests live next to source files under `frontend/src`.

The Playwright smoke test lives under `frontend/tests/e2e` so it resolves the frontend package's
local `@playwright/test` dependency.
